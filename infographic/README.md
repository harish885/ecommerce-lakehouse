# Azure E-Commerce Lakehouse Infographic

Run `npm install puppeteer && node render.js` from this folder to export `infographic.html` as `infographic.png` at 1200×3600 px with `deviceScaleFactor: 2`, producing a Retina-quality 2400×7200 PNG.
